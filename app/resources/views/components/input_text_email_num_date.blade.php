<div class="form-group mb-3">
    <label for="{{$input_id}}">{{$label}}</label>
    <input type="{{$type}}" class="form-control" id="{{$input_id}}" name="{{$input_id}}" value="{{$value}}"
        placeholder="{{$placeholder}}" {{$required}} />
</div>
