<div class="form-group mb-3">
    <label for="{{$input_id}}">{{$label}}</label>
    <textarea class="form-control" rows="{{$rows}}" id="{{$input_id}}" name="{{$input_id}}"
        placeholder="{{$placeholder}}" {{$required}}>
    </textarea>
</div>
